
interface Window {
    ethereum?: any
}

interface Transaction {
    hash: string
    amount: string
    blockNumber: number
}

interface FeatureCardProps {
    icon: any;
    title: string;
    description: string;
}